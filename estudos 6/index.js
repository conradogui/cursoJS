/*function falaOi () {
    return 'oi'
}
const vaiExecutar = false // aqui seria um conjunto de códigos que avaliariam se é true ou false, mas ja botei false para exemplificar

console.log(vaiExecutar && falaOi()) //retorna o valor false //caso as duas expressoes fossem verdadeiras iria retornar a ultima 
*/
console.log(0 || false || null || 'Conrado' || true) // nesse caso retorna o primeiro valor verdadeiro que é encontrado (no caso 'Conrado)
