import click from './jogopt1'

const rand = (max, min) => Math.floor(Math.random() * (max - min) + min)

function comput () {
    const objetos = document.querySelector('.objetos')
    for(let i = 0; i.length; i++) {
        i = objetos.rand(0, 3)
    }
}




